# Section 4 rebuild

This experiment rebuilds the three-mode chapter around two parallel,
predeclared analyses:

- a normalized, held-out validated stationary-density calculation for
  Section 4.1;
- held-out validation and synthesis of controlled autocorrelation/EDMD modes
  for Section 4.3.

The frozen scientific protocol is [`PROTOCOL.md`](PROTOCOL.md).  Section 4.2
will be rewritten only after the stationary-density verdict fixes the allowed
strength of its phase-selection and current-balance claims.

Primary input data are read-only files under
`../spectral_gap_controlled_2026-09-07/raw`.  No new simulation is authorized
by the primary protocol.
