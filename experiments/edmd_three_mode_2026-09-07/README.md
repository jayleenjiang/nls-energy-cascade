# Three-mode EDMD analysis

Analysis-only experiment using the completed controlled Cartesian trajectories
from `experiments/spectral_gap_controlled_2026-09-07/raw`.

The scientific and numerical choices are frozen in `PROTOCOL.md` before the
first EDMD spectrum is computed.  Run with:

```bash
python3 run_edmd.py --input ../spectral_gap_controlled_2026-09-07/raw --output analysis
```

