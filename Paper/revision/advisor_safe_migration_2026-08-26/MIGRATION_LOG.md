# Safe migration log

Date: 2026-08-26

Baseline: `/Users/jayleenjiang/Downloads/draft (1).tex`

Candidate enhanced source: `/Users/jayleenjiang/Downloads/main (7).tex`

Policy: migrate writing, organization, definitions, and verified consistency
corrections. Do not migrate a numerical claim unless it is already present in
the advisor-synced baseline or explicitly requested by the advisor.

## Included in this pass

1. Added a literature-positioning subsection separating resonant-NLS action
   transport from physical heat transport in oscillator chains.
2. Rewrote the contributions paragraph to remove unsynced claims of an
   insulating exponent, entropy-production measurements, and an overdamped
   generator spectrum.
3. Renamed the profile and conductivity language from energy/thermal to
   action/action-current where the continuity equation concerns
   `M = sum_j I_j`.
4. Corrected the Fourier-law sign convention by defining
   `grad T = (T_n-T_1)/n < 0` and
   `kappa_n = -E[J(n)]/grad T`.
5. Corrected the full-coordinate angular diffusion coefficient from
   `4 gamma T/I` to `gamma T/I`; retained `4 gamma T/I` only for a doubled
   boundary bond angle.
6. Corrected the left-boundary phase-drift sign for
   `theta_2 = 2(phi_2-phi_1)` and wrote the right-boundary sign explicitly.
   This was cross-checked against:
   - `flux/NLS_flux_canonical.cpp`, lines 235--255;
   - `flux/flux_data/NLS_flux_SIMD.cpp`, lines 219--245;
   - `lte/code/lte_histogram.cpp`, lines 193--208.
7. Added the equal-temperature Gibbs consistency proposition only after the
   boundary sign and diffusion coefficients were made consistent with the
   stated angle convention.
8. Recast the eigenfunction procedure as an observable-dependent slow-mode
   diagnostic, not a proof of the full spectral gap or a real spectrum.
9. Added a numerical-validation protocol without importing any validation
   numbers from the enhanced draft.
10. Replaced the LTE residual placeholder with the requested three-dimensional
    mesh generated by `compare_residual.m`. The plotting fit uses bins with
    both counts above 50 and is explicitly distinguished from the
    core-weighted fit in the LTE table.
11. Reframed entropy production as out of scope until a separate boundary-energy
    accumulator is implemented.
12. Added the three literature entries needed by the positioning subsection:
    Dhar (2008), Spohn (2014), and Lebowitz--Spohn (1999).

## Deliberately not migrated

- The canonical current-scaling table, exponent, bootstrap interval, or
  insulating-conductivity conclusion from the enhanced draft.
- Gamma, bath-contrast, timestep, fit-window, or large-chain robustness tables.
- Any `n=50,60` extension claim.
- Pilot finite-time flux-distribution or large-deviation numbers based on 1024
  trajectories.
- Results from the production burn-in/flux run currently being generated.
- Any claim that the full generator spectrum is real or that a fitted decay is
  a window-independent spectral gap.
- Declarations and data-availability text containing paths or claims that have
  not yet been approved.
- The enhanced abstract or conclusion, because both depend on numerical claims
  intentionally excluded above.

## Still incomplete

- Abstract (`TODO`).
- Action-current conductivity results (`TODO`).
- Conclusion and limitations (`TODO`).
- Integration of the completed production burn-in and finite-time flux study,
  including uncertainty and dependence on both averaging time and threshold.

## Verification

- TeX Live build succeeds and produces a 15-page PDF.
- All eight required figures are present.
- The citation-key set and bibliography-key set match exactly (11 each).
- No undefined citations or references were reported.
- No overfull or underfull boxes were reported.
- PDF pages were rendered and visually checked; the LTE mesh is legible and is
  not clipped.

